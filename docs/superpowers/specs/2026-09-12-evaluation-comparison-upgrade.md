# Evaluation comparison upgrade

User authorization: expand beyond200 questions, show total elapsed time for comparison tables, show only devices that contributed to this experiment, and provide3-minute judge demonstration advice. This is an extension to the already approved and implemented system; existing model/worker/credit/lease semantics remain.

- Preserve fixed200 validation dataset and probe. Add full1172-row official ARC-Challenge test split, fixed content snapshot, deterministic seed42 order, source/license/hashes. No training examples in evaluation rows. Raise sample ceiling to2000 while keeping2MB upload cap. Offer20/200/500/1000/all plus numeric count in available bounds. Same ordered prefix must be used for repeatable comparisons.
- Add coordinator-derived createdAt, elapsedMs (publish to now/end), executionElapsedMs (first dispatch to now/end; null when never dispatched). Finish/cancel freezes both elapsed counters. Pure cache runs have total time but no execution duration. No sum-of-node times masquerading as wall-clock time.
- Contributing devices means accepted newly computed results in THIS experiment. Remove historical unrelated sessions and source workers of cached results. Keep actual contributors after leaving. Do not delete worker/result audit records. Expose a separate authenticated current-pool endpoint for readiness/benchmark discovery, so filtering contributors does not break held benchmarks.
- Show precise elapsed time in dashboard and report. Export one-row CSV summary for spreadsheet comparisons, including experiment identity, state, mode, dataset, counts, contributors, timing, failures/retries, model fingerprint and a workload fingerprint based on ordered computation keys. Imported old JSON reports must remain viewable; derive only timestamps actually present or show unavailable.
- Explain a repeatable strongest-single / pooled-dynamic comparison with same workload, warm models, equal fixed contribution settings, cache-free benchmark mode, repeated runs, and separate exit/recovery demonstration. No unmeasured speedup/energy claim.

Validation: test timing before dispatch/running/completed/canceled/cache and historical stability; prove contributor filtering including cache case and cross-experiment registration; benchmark readiness must work with zero contributors. Validate added1172 unique real rows, train/validation separation and hashes. Exercise quantity selection and CSV quoting/formula safety/old report compatibility. Run all tests/typecheck/build and browser smoke against existing data. No Git repository exists. Preserve live data and restart coordinator only after checks, preferably with no pending work.
